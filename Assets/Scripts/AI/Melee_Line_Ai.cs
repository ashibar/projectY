using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Melee_Line_Ai : Melee_base_Ai
{
    [SerializeField] private float dir_X;
    [SerializeField] private float dir_Y;
    public override void ai_process()
    {
        //Vector2 vec_nor = 
        //transform.position = Vector2.MoveTowards(transform.position,transform.position + (Vector3) dir,speed * Time.deltaTime);
    }
}
